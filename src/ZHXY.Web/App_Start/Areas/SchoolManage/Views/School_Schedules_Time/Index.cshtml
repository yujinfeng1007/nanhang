@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Index.cshtml";
}
<script>
    $(function () {
        gridList();
        $("#F_Divis").bindSelect({
            url: "/SystemManage/Organize/GetSelectJsonByCategoryId?keyword=Division",
            displayBlank: false
        });
        $("#F_Divis").change(function () {
            $("#F_Grande").find("option").remove();
            $("#F_Grande").append("<option value=''>选择年级</option>");
            $("#F_Grande").bindSelect({
                url: "/SystemManage/Organize/GetSelectJsonByCategoryId?keyword=Grade&parentId=" + $("#F_Divis").val(),
                displayBlank: false
            });
        });
        $("#F_Semester").bindSelect({
            url: "/SchoolManage/School_Semester/GetGridSelect",
            id: "F_Id",
            text: "F_Name",
            displayBlank: false
        });
    })
    function gridList() {
        var $gridList = $("#gridList");
        $gridList.dataGrid({
            url: "/SchoolManage/School_Schedules_Time/GetGridJson",
            height: $(window).height() - 128,
            colModel: [

			 	{ label: '主键', name: 'F_Id', hidden: true },

                //{ label: '序号', name: 'F_SortCode', width: 80, align: 'left' },

                //{ label: '所属部门', name: 'F_DepartmentId', width: 80, align: 'left' },

                //{ label: '删除标记', name: 'F_DeleteMark', width: 80, align: 'left' },

			 	//{ label: "启用状态", name: "F_EnabledMark", width: 60, align: "center",
                //   formatter: function (cellvalue, options, rowObject) {
                //        if (cellvalue == 1) {
                //            return '<span class=\"label label-success\">正常</span>';
                //        } else if (cellvalue == 0) {
                //            return '<span class=\"label label-default\">禁用</span>';
                //        }
                //    }
                //},

			 	//{ label: '创建时间', name: 'F_CreatorTime', width: 80, align: 'left',
                //    formatter: "date", formatoptions: { srcformat:'Y-m-d H:i:s',newformat:'Y-m-d H:i:s' }
                //},

                //{ label: '创建者', name: 'F_CreatorUserId', width: 80, align: 'left' },

                //{ label: '备注', name: 'F_Memo', width: 80, align: 'left' },

                //{ label: '年级', name: 'F_Grande', width: 80, align: 'left' },

                //{ label: '学校', name: 'F_School', width: 80, align: 'left' },

                //{ label: '开始时间', name: 'F_Course_StartTime1', width: 80, align: 'left' },

                //{ label: '结束时间', name: 'F_Course_EndTime1', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan1', width: 80, align: 'left' },

                //{ label: '开始时间2', name: 'F_Course_StartTime2', width: 80, align: 'left' },

                //{ label: '结束时间2', name: 'F_Course_EndTime2', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan2', width: 80, align: 'left' },

                //{ label: '开始时间3', name: 'F_Course_StartTime3', width: 80, align: 'left' },

                //{ label: '结束时间3', name: 'F_Course_EndTime3', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan3', width: 80, align: 'left' },

                //{ label: '开始时间4', name: 'F_Course_StartTime4', width: 80, align: 'left' },

                //{ label: '结束时间4', name: 'F_Course_EndTime4', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan4', width: 80, align: 'left' },

                //{ label: '开始时间5', name: 'F_Course_StartTime5', width: 80, align: 'left' },

                //{ label: '结束时间5', name: 'F_Course_EndTime5', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan5', width: 80, align: 'left' },

                //{ label: '开始时间6', name: 'F_Course_StartTime6', width: 80, align: 'left' },

                //{ label: '结束时间6', name: 'F_Course_EndTime6', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan6', width: 80, align: 'left' },

                //{ label: '开始时间7', name: 'F_Course_StartTime7', width: 80, align: 'left' },

                //{ label: '结束时间7', name: 'F_Course_EndTime7', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan7', width: 80, align: 'left' },

                //{ label: '开始时间8', name: 'F_Course_StartTime8', width: 80, align: 'left' },

                //{ label: '结束时间8', name: 'F_Course_EndTime8', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan8', width: 80, align: 'left' },

                //{ label: '开始时间9', name: 'F_Course_StartTime9', width: 80, align: 'left' },

                //{ label: '结束时间9', name: 'F_Course_EndTime9', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan9', width: 80, align: 'left' },

                //{ label: '开始时间10', name: 'F_Course_StartTime10', width: 80, align: 'left' },

                //{ label: '结束时间10', name: 'F_Course_EndTime10', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan10', width: 80, align: 'left' },

                //{ label: '开始时间11', name: 'F_Course_StartTime11', width: 80, align: 'left' },

                //{ label: '结束时间11', name: 'F_Course_EndTime11', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan11', width: 80, align: 'left' },

                //{ label: '开始时间12', name: 'F_Course_StartTime12', width: 80, align: 'left' },

                //{ label: '结束时间12', name: 'F_Course_EndTime12', width: 80, align: 'left' },

                //{ label: '时段', name: 'F_TimeSpan12', width: 80, align: 'left' },
                {
                    label: '所属学部', name: 'F_Divis', width: 80, align: 'left', formatter: function (cellvalue) {
                        return top.clients.organize[cellvalue] == null ? "" : top.clients.organize[cellvalue].fullname;
                    }
                },
                {
                    label: '所属年级', name: 'F_Grande', width: 80, align: 'left', formatter: function (cellvalue) {
                        return top.clients.organize[cellvalue] == null ? "" : top.clients.organize[cellvalue].fullname;
                    }
                },
                {
                    label: '所属学期', name: 'F_Semester', width: 80, align: 'left',
                    formatter: function (cellvalue, options, rowObject) {
                        var F_Semester;
                        $.ajax({
                            url: "/SchoolManage/School_Semester/GetFormJson?keyValue=" + cellvalue,
                            dataType: "json",
                            async: false,
                            success: function (data) {
                                F_Semester = data["F_Name"];
                            }
                        });
                        return F_Semester;
                    }
                },
            ],
            pager: "#gridPager",
            sortname: 'F_DepartmentId asc,F_CreatorTime desc',
            viewrecords: true
        });
        $("#btn_search").click(function () {
            $gridList.jqGrid('setGridParam', {
                postData: { keyword: $("#txt_keyword").val(), F_Divis: $("#F_Divis").val(), F_Grande: $("#F_Grande").val(), F_Semester: $("#F_Semester").val() },
            }).trigger('reloadGrid');
        });
    }
    function btn_add() {
        $.modalOpen({
            id: "Form",
            title: "新增",
            url: "/SchoolManage/School_Schedules_Time/Form",
            width: "1200px",
            height: "910px",
            callBack: function (iframeId) {
                top.frames[iframeId].submitForm();
            }
        });
    }
    function btn_edit() {
        var keyValue = ($("#gridList").jqGridRowValue('F_Id'))[0];
        $.modalOpen({
            id: "Form",
            title: "修改",
            url: "/SchoolManage/School_Schedules_Time/Form?keyValue=" + keyValue,
            width: "1200px",
            height: "910px",
            callBack: function (iframeId) {
                top.frames[iframeId].submitForm();
            }
        });
    }
    function btn_delete() {
        var deleteid = $('#gridList').jqGridRowValue('F_Id');
        var keyValue = "";
        for (var i = 0; i < deleteid.length; i++) {
            keyValue += deleteid[i] + "|";
        }
        $.deleteForm({
            url: "/SchoolManage/School_Schedules_Time/DeleteForm",
            param: { keyValue: keyValue },
            success: function () {
                $.currentWindow().$("#gridList").trigger("reloadGrid");
            }
        })
    }
    function btn_details() {
        var keyValue = ($("#gridList").jqGridRowValue('F_Id'))[0];
        $.modalOpen({
            id: "Details",
            title: "查看",
            url: "/SchoolManage/School_Schedules_Time/Details?keyValue=" + keyValue,
            width: "1200px",
            height: "950px",
            btn: null,
        });
    }

    function btn_disabled() {
        var keyValue = $("#gridList").jqGridRowValue('F_Id');
        $.modalConfirm("注：您确定要【禁用】该项吗？", function (r) {
            if (r) {
                $.submitForm({
                    url: "/SchoolManage/School_Schedules_Time/DisabledAccount",
                    param: { keyValue: keyValue },
                    success: function () {
                        $.currentWindow().$("#gridList").trigger("reloadGrid");
                    }
                })
            }
        });
    }
    function btn_enabled() {
        var keyValue = $("#gridList").jqGridRowValue('F_Id');
        $.modalConfirm("注：您确定要【启用】该项吗？", function (r) {
            if (r) {
                $.submitForm({
                    url: "/SchoolManage/School_Schedules_Time/EnabledAccount",
                    param: { keyValue: keyValue },
                    success: function () {
                        $.currentWindow().$("#gridList").trigger("reloadGrid");
                    }
                })
            }
        });
    }

    function btn_export() {
        var keyValue = $("#txt_keyword").val();
        if (keyValue != null)
            keyValue = "?keyword=" + keyValue;

        document.location = "/SchoolManage/School_Schedules_Time/export" + keyValue;
    }

    function btn_import() {
        var title = 'Excel';
        var extensions = 'xls,xlsx';
        var mimeTypes = 'application/vnd.ms-excel,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet';
        $.modalOpen({
            id: "Form",
            title: "导入",
            url: "/Home/Import?title=" + title + "&extensions=" + extensions + "&mimeTypes=" + mimeTypes + "",
            width: "500px",
            height: "300px",
            callBack: function (iframeId) {
                var filePali = top.frames[iframeId].submitForm();
                importAcrion(filePath);
            }
        });
    }

    function importAcrion(filePath) {
        $.submitForm({
            url: "/SchoolManage/School_Schedules_Time/import",
            param: { filePath: filePath },
            success: function () {
                $.currentWindow().$("#gridList").trigger("reloadGrid");
            }
        })
    }
</script>

<div class="topPanel">
    <div class="toolbar">
        <div class="btn-group">
            <a class="btn btn-primary" onclick="$.reload()"><span class="glyphicon glyphicon-refresh"></span></a>
        </div>
        <div class="btn-group">
            <a id="NF-add" authorize="yes" class="btn btn-primary dropdown-text" onclick="btn_add()"><i class="fa fa-plus"></i>新建</a>
        </div>
        <div class="btn-group"><a id="NF-export" authorize="yes" class="btn btn-primary dropdown-text" onclick="btn_export()"><i class="fa fa-arrow-down"></i>导出</a></div>
        <div class="btn-group"><a id="NF-import" authorize="yes" class="btn btn-primary dropdown-text" onclick="btn_import()"><i class="fa fa-arrow-up"></i>导入</a></div>
            <div class="operate">
                <ul class="nav nav-pills">
                    <li class="first">已选中<span>1</span>项</li>
                    <li><a id="NF-edit" authorize="yes" onclick="btn_edit()"><i class="fa fa-pencil-square-o"></i>修改</a></li>
                    <li><a class="multi-edit" id="NF-delete" authorize="yes" onclick="btn_delete()"><i class="fa fa-trash-o"></i>删除</a></li>
                    <li><a id="NF-Details" authorize="yes" onclick="btn_details()"><i class="fa fa-search-plus"></i>查看</a></li>
                    <li class="split"></li>
                    <li><a id="NF-revisepassword" authorize="yes" onclick="btn_revisepassword()"><i class="fa fa-key"></i>密码重置</a></li>
                    <li class="split"></li>
                    <li><a class="multi-edit" id="NF-disabled" authorize="yes" onclick="btn_disabled()"><i class="fa fa-stop-circle"></i>禁用</a></li>
                    <li><a class="multi-edit" id="NF-enabled" authorize="yes" onclick="btn_enabled()"><i class="fa fa-play-circle"></i>启用</a></li>
                </ul>
                <a href="javascript:;" class="close"></a>
            </div>
            <script>$('.toolbar').authorizeButton()</script>
        </div>
    <div class="search">
        <ul>
            <li class="formTitle">搜索：</li>
            <li>
                <div class="input-group" style="margin-right:10px;width:200px;">
                    <select id="F_Divis" name="F_Divis" class="form-control">
                        <option value="">选择学部</option>
                    </select>
                </div>
            </li>
            <li>
                <div class="input-group" style="margin-right:10px;width:200px;">
                    <select id="F_Grande" name="F_Grande" class="form-control">
                        <option value="">选择年级</option>
                    </select>
                </div>
            </li>
            <li>
                <div class="input-group" style="margin-right:10px;width:200px;">
                    <select id="F_Semester" name="F_Semester" class="form-control">
                        <option value="">选择学期</option>
                    </select>
                </div>
            </li>
            <li>
                <div class="input-group">
                    <span class="input-group-btn">
                        <button id="btn_search" type="button" class="btn  btn-primary"><i class="fa fa-search"></i></button>
                    </span>
                </div>
            </li>
        </ul>
    </div>
</div>
<div class="gridPanel">
    <table id="gridList"></table>
    <div id="gridPager"></div>
</div>